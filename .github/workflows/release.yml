name: Release

# Cut a release by pushing a tag like v1.0.0:
#   git tag v1.0.0 && git push origin v1.0.0
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Version tag to build (e.g. v1.0.0)"
        required: true

permissions:
  contents: write # create releases

jobs:
  release:
    runs-on: macos-15
    env:
      VERSION: ${{ github.event.inputs.tag || github.ref_name }}
      TAP_TOKEN: ${{ secrets.TAP_GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Select latest stable Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Import Developer ID signing certificate
        env:
          BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          set -euo pipefail
          CERT_PATH="$RUNNER_TEMP/cert.p12"
          KEYCHAIN="$RUNNER_TEMP/app-signing.keychain-db"
          echo "$BUILD_CERTIFICATE_BASE64" | base64 --decode > "$CERT_PATH"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security import "$CERT_PATH" -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k "$KEYCHAIN" -T /usr/bin/codesign
          security list-keychain -d user -s "$KEYCHAIN" $(security list-keychains -d user | sed s/\"//g)
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN" >/dev/null

      - name: Archive & export (Developer ID, deep-signed)
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail
          cat > /tmp/ExportOptions.plist <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key><string>developer-id</string>
            <key>signingStyle</key><string>manual</string>
            <key>teamID</key><string>${APPLE_TEAM_ID}</string>
          </dict>
          </plist>
          PLIST
          xcodebuild archive \
            -project ClipboardManager.xcodeproj \
            -scheme ClipboardManager \
            -configuration Release \
            -archivePath build/ClipboardManager.xcarchive \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Developer ID Application" \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID"
          xcodebuild -exportArchive \
            -archivePath build/ClipboardManager.xcarchive \
            -exportPath build/export \
            -exportOptionsPlist /tmp/ExportOptions.plist
          echo "APP_PATH=build/export/ClipboardManager.app" >> "$GITHUB_ENV"

      - name: Notarize & staple
        env:
          AC_API_KEY_BASE64: ${{ secrets.AC_API_KEY_BASE64 }}
          AC_API_KEY_ID: ${{ secrets.AC_API_KEY_ID }}
          AC_API_ISSUER_ID: ${{ secrets.AC_API_ISSUER_ID }}
        run: |
          set -euo pipefail
          KEY_PATH="$RUNNER_TEMP/ac_api_key.p8"
          echo "$AC_API_KEY_BASE64" | base64 --decode > "$KEY_PATH"
          mkdir -p dist
          ZIP="dist/ClipboardManager.zip"
          ditto -c -k --keepParent "$APP_PATH" "$ZIP"
          SUBMIT_JSON="$(xcrun notarytool submit "$ZIP" \
            --key "$KEY_PATH" --key-id "$AC_API_KEY_ID" --issuer "$AC_API_ISSUER_ID" \
            --wait --output-format json)"
          echo "$SUBMIT_JSON"
          SUBMIT_ID="$(echo "$SUBMIT_JSON" | /usr/bin/python3 -c 'import sys,json;print(json.load(sys.stdin)["id"])')"
          STATUS="$(echo "$SUBMIT_JSON" | /usr/bin/python3 -c 'import sys,json;print(json.load(sys.stdin)["status"])')"
          echo "Notarization status: $STATUS"
          # Always dump Apple's audit log — shows exactly what was rejected if Invalid.
          xcrun notarytool log "$SUBMIT_ID" \
            --key "$KEY_PATH" --key-id "$AC_API_KEY_ID" --issuer "$AC_API_ISSUER_ID" || true
          if [ "$STATUS" != "Accepted" ]; then
            echo "::error::Notarization failed with status $STATUS"
            exit 1
          fi
          xcrun stapler staple "$APP_PATH"
          rm "$ZIP"
          ditto -c -k --keepParent "$APP_PATH" "$ZIP"
          SHA="$(shasum -a 256 "$ZIP" | awk '{print $1}')"
          echo "ZIP_PATH=$ZIP" >> "$GITHUB_ENV"
          echo "SHA256=$SHA" >> "$GITHUB_ENV"
          {
            echo "### Release artifact"
            echo "- version: \`$VERSION\`"
            echo "- sha256: \`$SHA\`"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION }}
          files: ${{ env.ZIP_PATH }}
          generate_release_notes: true

      # Optional: auto-bump the Homebrew cask. Runs only if TAP_GITHUB_TOKEN is set
      # (a PAT with write access to thkobierecki/homebrew-tap). Otherwise skipped,
      # and you update the cask manually with the sha256 from the job summary.
      - name: Update Homebrew tap
        if: env.TAP_TOKEN != ''
        run: |
          set -euo pipefail
          V="${VERSION#v}"
          git clone "https://x-access-token:${TAP_TOKEN}@github.com/thkobierecki/homebrew-tap.git" tap
          CASK="tap/Casks/clipboard-manager.rb"
          /usr/bin/sed -i '' -E "s/version \".*\"/version \"$V\"/" "$CASK"
          /usr/bin/sed -i '' -E "s/sha256 \".*\"/sha256 \"$SHA256\"/" "$CASK"
          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit -am "clipboard-manager $V"
          git push
